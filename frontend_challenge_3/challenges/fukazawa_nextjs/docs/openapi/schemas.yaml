Company:
  type: object
  properties:
    id:
      type: integer
      description: エネルギー会社ID
    name:
      type: string
      description: エネルギー会社名
    area_code:
      type: integer
      description: エリアID
    area_name:
      type: string
      description: エリア
  required:
    - id
    - name
    - area_code
    - area_name

Plan:
  type: object
  properties:
    id:
      type: integer
      description: プランID
    name:
      type: string
      description: プラン名
    capacities:
      type: array
      items:
        $ref: '#/Capacity'
  required:
    - id
    - name
    - company_id
    - company_name


Capacity:
  type: object
  properties:
    #id:
    #  type: integer
    #  description: 契約容量ID
    name:
      type: string
      description: 契約容量名
    #plan_id:
    #  type: string
    #  description: プランID
  required:
    #- id
    - name
    #- plan_id


Companies:
  type: array
  items:
    $ref: '#/Company'

Plans:
  type: array
  items:
    $ref: '#/Plan'

Capacities:
  type: array
  items:
    $ref: '#/Capacity'


Error:
  type: object
  properties:
    code:
      type: string
    message:
      type: string
  required:
    - code
    - message